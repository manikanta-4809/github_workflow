name: Manual and Scheduled Workflows
on:
  workflow_dispatch:
    # ... (inputs from above) ...
  schedule:
    # Use a tool like crontab.guru to build these schedules
    # Runs at 02:00 UTC every Monday
    - cron: '0 2 * * 1'
    # Runs every hour
